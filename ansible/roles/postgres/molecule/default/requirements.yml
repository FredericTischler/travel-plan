---
# Collections requises par le role et par le scenario Molecule (driver docker).
collections:
  - name: community.postgresql
    version: ">=4.2.0,<5.0.0"
  - name: community.docker
    version: ">=5.2.1,<6.0.0"
  - name: ansible.posix
    version: ">=2.2.0,<3.0.0"